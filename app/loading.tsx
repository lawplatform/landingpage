import Loading_basic from "@/src/components/Loading_basic";

export default function Loading() {
	// You can add any UI inside Loading, including a Skeleton.
	return <Loading_basic></Loading_basic>
}
